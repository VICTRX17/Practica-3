#include <stdio.h>

int main() {
    int var1 = 3;     // Variable sencera amb valor inicial 3
    char var2 = 'a';     // Variable de tipus caràcter amb valor inicial 'a' (ASCII 97)
    int result;              // Variable per emmagatzemar el resultat

    // Producte
    result = var1 * var2;

    return 0;
}
